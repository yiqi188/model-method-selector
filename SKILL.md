---
name: model-method-selector
description: 基于题目结构、数据特征和论文证据，筛选、复现、比较并推荐适合当前问题的解题方法和数学模型。适用于论文方法比较、候选模型评估、已发表方法迁移，以及数据驱动或数学建模问题的模型选择。
---

# 建模方法筛选器

## 核心原则

按“题目结构优先、论文证据辅助、同条件实验定论”的顺序工作。

- 题目结构决定候选模型类别，不能因为某篇论文使用了某个算法就直接照搬。
- 论文摘要只用于发现候选方法和研究思路，不能单独证明某模型更优秀。
- 只有在同一数据、同一目标、同一约束和同一评价指标下复现比较，才能称为经验上更优。
- 始终区分三类结论：文献明确报告的事实、根据题目结构的理论判断、通过复现实验得到的结果。

## 输入材料与证据等级

先检查材料是否足够，并在内部分析中记录证据等级。只有用户明确要求查看比较过程时，才在输出中展示：

- **A级**：完整论文、公式、实验设置、数据或代码齐全，可以复现。
- **B级**：有完整方法和实验结果，但缺少代码或原始数据，只能部分复现。
- **C级**：只有摘要、截图或结论，只能提取候选方法，不能证明优劣。
- **D级**：只有题目，没有论文；可以独立建模，但不能声称符合某篇论文。

最小输入是题目说明和数据。若要进行论文对比，优先使用全文中“模型建立、算法、实验结果、局限性”部分；只有摘要时，在内部标记材料不足，并在大纲相关位置标注缺失条件。

## 工作流程

### 1. 定义当前问题

提取目标、输出、决策变量、状态变量、约束、数据类型、样本规模、随机性、评价指标和计算资源。缺失关键条件时列出假设，不要默默补全。

先判断问题属于预测、分类、评价、优化、分配、路径、时间序列、因果分析、抽样检验或多阶段决策。

### 2. 建立题目驱动的基线

在阅读论文方法前，先根据题目结构提出至少一个可解释基线：

- 概率或抽样问题：从二项、泊松、正态、Beta-Binomial 等合适分布开始；
- 少量离散决策：优先考虑全枚举、动态规划或精确整数规划；
- 多阶段和回流决策：考虑随机动态规划、MDP 或吸收马尔可夫链；
- 大规模组合优化：再考虑分支定界、MILP、遗传算法、模拟退火或蚁群算法。

记录基线的假设、目标函数、约束和验证方式。论文中的方法必须与基线比较，而不是替代题目分析。

### 3. 筛选和读取论文

按相关性、假设匹配度、数据可得性、方法透明度、实验完整性和可复现性筛选论文。引用量、期刊级别或论文声称的“最优”不能单独作为模型质量证据。

逐篇提取以下字段，并给出原文页码、段落、表格或文件名：

| 字段 | 需要记录的内容 |
|---|---|
| 研究问题 | 目标、决策对象和应用场景 |
| 数据 | 来源、规模、变量、分布、缺失值和预处理 |
| 假设 | 独立性、平稳性、先验、线性、正态性等 |
| 数学模型 | 状态、变量、目标函数、约束、概率转移 |
| 求解算法 | 精确算法、启发式算法、仿真或混合算法 |
| 验证 | 训练/测试划分、基准、指标、重复次数、敏感性分析 |
| 结果 | 只记录原文明确报告的数值和结论 |
| 局限 | 失效条件、数据依赖、计算成本和未解决问题 |

明确区分“模型”和“算法”：例如回归、排队、动态规划状态模型是模型；遗传算法、模拟退火、线性规划求解器是求解方法。

### 4. 判断方法能否迁移

逐个检查候选方法是否满足当前题目的：

- 目标和约束是否一致；
- 数据类型、规模和分布是否一致；
- 关键假设是否成立；
- 是否能处理随机性、回流、缺失和不确定参数；
- 是否能输出题目要求的决策和指标；
- 结果是否可解释、可验证、可复现；
- 计算量是否适合当前规模。

将每项标记为“满足、部分满足、不满足或未知”，未知项不能当作满足处理。

### 5. 设计同条件比较

只有具备数据或可复现的模拟场景时，才进行模型优劣比较。统一以下条件：

- 相同数据、预处理、训练/测试划分或随机场景；
- 相同目标函数、约束、参数预算和停止条件；
- 相同评价指标和单位；
- 启发式算法使用多个随机种子，报告均值、标准差和最好/最差结果；
- 与可求精确解的基线比较最优性差距；
- 做参数敏感性、扰动实验和失败案例分析；
- 同时记录计算时间、内存、可解释性和复现难度。

建议使用多指标评分，而不是只看一个结果：

```text
总评分 = 适配度 + 数据可得性 + 可解释性 + 稳健性 + 最优性证据 - 计算成本
```

在内部分析中记录权重和评分依据。没有复现实验时，不得判定“实验优胜者”，只能形成理论适配性判断和相应证据等级。

### 6. 形成推荐

在内部形成首选模型、备选模型和不推荐模型。每个推荐必须回答：

1. 它解决了当前问题的哪个结构；
2. 依据是论文事实、理论推导还是复现实验；
3. 它依赖哪些假设和数据；
4. 在什么情况下会失效；
5. 如何验证或替换它。

若多个模型互补，可以组合为“精确基线 + 大规模启发式 + 不确定性分析”，但必须说明每一层的职责，不能把多个算法堆在一起作为理由。

## 生产质量与回流决策的专项建模路线

当题目涉及抽样检测、生产质量控制、拆解回流、多阶段装配或次品率不确定性时，将以下四层模型纳入候选路线，并根据题目条件和同条件验证决定最终组合。

### 1. 精确抽样判断

令抽样次品数 `X_n ~ Binomial(n,p)`。不要对 Bernoulli 次品比例直接套用 t 分布。

- **序贯判断**：每增加一批样本后计算精确单侧置信界。若下置信限高于标称次品率则拒收；若上置信限不超过标称值则接收；否则继续抽样。
- **固定 `(n,c)` 方案**：明确可接受质量水平、不可接受质量水平、生产者风险和消费者风险，在精确二项尾概率约束下最小化整数样本量 `n`。
- **最少期望样本量**：存在明确的可接受/不可接受次品率时，使用 SPRT，并报告 OC 曲线和平均样本数 ASN 曲线。

若题目没有给出另一侧错误概率、检验功效或不可接受质量水平，说明固定最小样本量不唯一，不要人为补出唯一答案。

### 2. 少量生产决策的精确模型

把购买件、拆解回收件、半成品和成品分别建状态。状态至少包含来源、是否检测、质量状态和生产阶段，避免未检测次品无限回流。

使用有限状态 MDP：

```text
V(s) = max_a [r(s,a) + sum(P(s'|s,a)V(s'))]
```

即时收益统一包含销售收入、购买、检测、装配、拆解、报废和调换损失。固定策略下可用吸收马尔可夫链：

```text
V_pi = (I-Q_pi)^(-1)r_pi
```

- 4 位策略完整枚举 16 种；
- 若回收件单独决策，6 位策略完整枚举 64 种；
- 主指标统一为每交付一件合格成品的期望净利润，同时报告次品流入市场概率和调换损失。

不要使用蚁群、遗传或模拟退火搜索几十个可以完整枚举的策略。

### 3. 多阶段装配树动态规划

将零件作为叶节点、半成品作为中间节点、成品作为根节点。每个节点记录来源、检测状态、质量状态、数量和工序阶段。

- 从叶到根计算检测、装配、拆解和报废动作的期望成本、合格产出率和回收价值；
- 使用随机动态规划和记忆化保存重复状态；
- 决策位数较小时先完整枚举 `2^d` 个策略，以枚举最优值校验动态规划；
- 中等规模使用分支定界或 MILP；只有精确方法超时后才使用启发式算法。

启发式算法至少使用 30 个随机种子，报告均值、标准差、最好/最差结果、运行时间和相对精确基准的最优性差距。迭代曲线收敛不能作为全局最优证明。

### 4. Beta-Binomial 后验鲁棒决策

保留每类产品的真实抽样记录 `(n_j,x_j)`，建立：

```text
p_j ~ Beta(alpha_j,beta_j)
p_j | x_j,n_j ~ Beta(alpha_j+x_j, beta_j+n_j-x_j)
```

将 MDP 或动态规划中的固定次品率替换为后验预测概率，比较：

- 最大后验期望利润策略；
- 可信域内最坏利润最大的稳健策略；
- 低分位利润或 CVaR 最大的风险厌恶策略。

无可靠先验时使用 `Beta(1,1)` 或 Jeffreys 先验 `Beta(0.5,0.5)`，并做先验和样本量敏感性分析。不要只用样本比例、后验均值或 3σ 区间替代完整的不确定性。

### 5. 必做验证

- 检查所有状态转移概率之和为 1；
- 检查固定策略能够进入吸收状态，`I-Q_pi` 可逆；
- 用全枚举核对小规模动态规划最优值；
- 用蒙特卡洛复核解析期望，但不让仿真取代精确解；
- 对次品率、检测成本、调换损失、拆解成本和先验参数做敏感性分析；
- 检查样本量增大时后验区间收窄，随机模型的决策逐渐趋近确定参数模型。

## 默认交付规则

将论文比较、证据评估和候选筛选作为内部分析过程。除非用户明确要求查看比较过程，否则最终只输出融合后的最优建模思路/大纲，不展示论文编号、论文来源、证据等级、模型评分或方法淘汰过程。

## 建模思路/大纲输出格式

按以下顺序直接输出：

1. **总体建模框架**：说明各子问题的递进关系和完整技术路线。
2. **问题拆解**：逐问写明输入、输出、决策变量、目标和约束。
3. **关键假设**：列出必要假设、采用理由和可能影响。
4. **符号与变量**：统一定义状态变量、决策变量、参数和评价指标。
5. **各子问题模型**：逐问给出模型名称、核心公式、目标函数、约束条件和模型间接口。
6. **求解算法**：说明算法步骤、停止条件、复杂度和适用规模。
7. **结果指标**：明确需要输出的数值、策略、图表和对比表。
8. **检验方案**：写出精确验算、仿真复核、敏感性、鲁棒性和误差分析。
9. **代码任务清单**：按子问题列出输入、输出、实现方法和校验方式。

内容必须具体到可交给代码阶段直接实现。若材料不足，在相应章节标注缺失参数和条件化方案，不要另写论文比较报告。

## 约束

- 不编造论文中不存在的模型、数据、数值、引用或实验结果。
- 不把摘要中的利润、准确率或“最优”表述当成跨论文可比的证据。
- 不因某种方法在论文中出现次数多就判定它更优。
- 不把启发式算法的一个随机运行结果当作全局最优证明。
- 不能仅凭摘要确认公式或参数；在大纲相应位置标注该信息无法由现有材料确认。
- 在内部将结论标记为“文献事实、理论判断、复现实验结果”之一；只有用户要求查看比较过程时才展示该标记。
- 若材料不足以比较，在大纲相应章节标注缺口并给出条件化方案，不要假装已经完成实验。
