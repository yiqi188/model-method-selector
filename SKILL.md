---
name: model-method-selector
description: 基于题目结构、数据特征和论文证据，筛选、复现、比较并推荐适合当前问题的解题方法和数学模型。Use when the user asks to compare papers, evaluate competing models, transfer a published method, or choose a model for a data-driven or mathematical modeling problem.
---

# 建模方法筛选器

## 核心原则

按“题目结构优先、论文证据辅助、同条件实验定论”的顺序工作。

- 题目结构决定候选模型类别，不能因为某篇论文使用了某个算法就直接照搬。
- 论文摘要只用于发现候选方法和研究思路，不能单独证明某模型更优秀。
- 只有在同一数据、同一目标、同一约束和同一评价指标下复现比较，才能称为经验上更优。
- 始终区分三类结论：文献明确报告的事实、根据题目结构的理论判断、通过复现实验得到的结果。

## 输入材料与证据等级

先检查材料是否足够，并在报告开头说明证据等级：

- **A级**：完整论文、公式、实验设置、数据或代码齐全，可以复现。
- **B级**：有完整方法和实验结果，但缺少代码或原始数据，只能部分复现。
- **C级**：只有摘要、截图或结论，只能提取候选方法，不能证明优劣。
- **D级**：只有题目，没有论文；可以独立建模，但不能声称符合某篇论文。

最小输入是题目说明和数据。若要进行论文对比，优先提供全文中“模型建立、算法、实验结果、局限性”部分；只有摘要时，明确提示需要补充材料。

## 工作流程

### 1. 定义当前问题

提取目标、输出、决策变量、状态变量、约束、数据类型、样本规模、随机性、评价指标和计算资源。缺失关键条件时列出假设，不要默默补全。

先判断问题属于预测、分类、评价、优化、分配、路径、时间序列、因果分析、抽样检验或多阶段决策。

### 2. 建立题目驱动的基线

在阅读论文方法前，先根据题目结构提出至少一个可解释基线：

- 概率或抽样问题：从二项、泊松、正态、Beta-Binomial 等合适分布开始；
- 少量离散决策：优先考虑全枚举、动态规划或精确整数规划；
- 多阶段和回流决策：考虑随机动态规划、MDP 或吸收马尔可夫链；
- 大规模组合优化：再考虑分支定界、MILP、遗传算法、模拟退火或蚁群算法。

记录基线的假设、目标函数、约束和验证方式。论文中的方法必须与基线比较，而不是替代题目分析。

### 3. 筛选和读取论文

按相关性、假设匹配度、数据可得性、方法透明度、实验完整性和可复现性筛选论文。引用量、期刊级别或论文声称的“最优”不能单独作为模型质量证据。

逐篇提取以下字段，并给出原文页码、段落、表格或文件名：

| 字段 | 需要记录的内容 |
|---|---|
| 研究问题 | 目标、决策对象和应用场景 |
| 数据 | 来源、规模、变量、分布、缺失值和预处理 |
| 假设 | 独立性、平稳性、先验、线性、正态性等 |
| 数学模型 | 状态、变量、目标函数、约束、概率转移 |
| 求解算法 | 精确算法、启发式算法、仿真或混合算法 |
| 验证 | 训练/测试划分、基准、指标、重复次数、敏感性分析 |
| 结果 | 只记录原文明确报告的数值和结论 |
| 局限 | 失效条件、数据依赖、计算成本和未解决问题 |

明确区分“模型”和“算法”：例如回归、排队、动态规划状态模型是模型；遗传算法、模拟退火、线性规划求解器是求解方法。

### 4. 判断方法能否迁移

逐个检查候选方法是否满足当前题目的：

- 目标和约束是否一致；
- 数据类型、规模和分布是否一致；
- 关键假设是否成立；
- 是否能处理随机性、回流、缺失和不确定参数；
- 是否能输出题目要求的决策和指标；
- 结果是否可解释、可验证、可复现；
- 计算量是否适合当前规模。

将每项标记为“满足、部分满足、不满足或未知”，未知项不能当作满足处理。

### 5. 设计同条件比较

只有具备数据或可复现的模拟场景时，才进行模型优劣比较。统一以下条件：

- 相同数据、预处理、训练/测试划分或随机场景；
- 相同目标函数、约束、参数预算和停止条件；
- 相同评价指标和单位；
- 启发式算法使用多个随机种子，报告均值、标准差和最好/最差结果；
- 与可求精确解的基线比较最优性差距；
- 做参数敏感性、扰动实验和失败案例分析；
- 同时记录计算时间、内存、可解释性和复现难度。

建议使用多指标评分，而不是只看一个结果：

```text
总评分 = 适配度 + 数据可得性 + 可解释性 + 稳健性 + 最优性证据 - 计算成本
```

报告权重和评分依据。没有复现实验时，不填写“实验优胜者”，只给出理论适配性和证据等级。

### 6. 形成推荐

输出首选模型、备选模型和不推荐模型。每个推荐必须回答：

1. 它解决了当前问题的哪个结构；
2. 依据是论文事实、理论推导还是复现实验；
3. 它依赖哪些假设和数据；
4. 在什么情况下会失效；
5. 如何验证或替换它。

若多个模型互补，可以组合为“精确基线 + 大规模启发式 + 不确定性分析”，但必须说明每一层的职责，不能把多个算法堆在一起作为理由。

## 数学建模决策题的优先级示例

以下是根据题目结构建立的优先级，不是对论文结果的盲目复述：

- **抽样检验**：精确二项单侧检验、Clopper-Pearson 区间；追求期望样本量时考虑 SPRT。固定样本量必须同时说明另一侧错误概率或检验功效。
- **少量生产决策**：期望利润 + 有限状态 MDP/吸收马尔可夫链 + 全策略枚举，优先得到可验证的精确解。
- **多阶段装配决策**：随机动态规划、分支定界或 MILP；问题规模较大时再用遗传算法、模拟退火或蚁群算法，并用小规模精确解校验。
- **抽样得到的次品率**：Beta-Binomial 后验更新、后验预测和鲁棒决策；不要只把样本比例或后验均值当作确定参数。

## 输出格式

按以下顺序输出：

1. **问题理解与证据等级**：目标、数据、约束、指标、假设和材料完整度。
2. **论文证据台账**：来源、原文定位、明确事实、推断内容和未知内容。
3. **候选模型对比表**：模型、算法、适用条件、假设、优点、局限和迁移判断。
4. **基线模型**：不依赖论文也能由题目结构推出的模型及理由。
5. **同条件实验设计**：数据划分、指标、随机种子、基准、敏感性分析和复现步骤。
6. **推荐结果**：首选、备选、不推荐、证据来源、置信程度和失效条件。
7. **落地方案**：变量、目标函数、约束、求解流程、验证和替代方案。
8. **证据缺口**：还需要的全文、数据、代码、参数或业务条件。

## 约束

- 不编造论文中不存在的模型、数据、数值、引用或实验结果。
- 不把摘要中的利润、准确率或“最优”表述当成跨论文可比的证据。
- 不因某种方法在论文中出现次数多就判定它更优。
- 不把启发式算法的一个随机运行结果当作全局最优证明。
- 不能仅凭摘要确认公式或参数时，写明“摘要未说明”。
- 结论必须标记为“文献事实、理论判断、复现实验结果”之一。
- 若材料不足以比较，先说明缺口，再给出条件化推荐，不要假装已经完成实验。
