---
name: model-method-selector
description: 比较多篇论文摘要、数据与实验条件，识别问题类型并筛选可行的解题方法和数学模型。Use when the user asks to compare papers, infer modeling approaches, choose models for a problem, or turn abstract evidence into a justified method recommendation.
---

# 建模方法筛选器

## 目标

根据用户提供的论文摘要、数据说明、题目背景或已有结果，归纳不同研究采用的方法和模型，判断它们是否适合当前问题，并给出有证据支撑的推荐方案。

## 工作流程

1. 明确当前问题的目标、输出、变量、约束、数据来源和评价指标。信息不足时列出假设，不要默默补全关键条件。
2. 逐篇读取摘要或材料，区分原文明确陈述、根据原文推断和暂时未知的信息。
3. 建立证据对比表，至少包含：来源、研究问题、数据类型与规模、关键变量、目标函数、约束、数学模型、求解算法、验证方式、主要结论和局限。
4. 统一同义术语，并区分“数学模型”和“求解算法”。例如回归模型是模型，遗传算法是求解或优化算法，不能混为一类。
5. 判断当前问题属于预测、分类、评价、优化、分配、路径、时间序列、因果分析或其他类型，并说明判断依据。
6. 从候选研究中筛选可迁移的方法，检查数据需求、假设、可解释性、鲁棒性、计算成本和结果可验证性。
7. 对候选方案进行定性或半定量评分，给出首选方案、备选方案和不推荐方案；每个判断都绑定到具体证据或明确假设。
8. 将首选方案落到可执行步骤：变量定义、模型形式、约束、参数估计或求解方法、验证指标和敏感性分析。
9. 检查证据是否足以支持推荐。若摘要缺少数据、指标或模型细节，明确标记“无法从现有材料确认”，并列出需要补充的材料。

## 输出格式

按以下顺序输出：

1. **问题理解**：目标、数据、约束、评价指标和必要假设。
2. **文献证据对比表**：逐篇列出方法、模型、数据和验证信息。
3. **候选方法与模型**：说明适用条件、优点、局限和所需数据。
4. **推荐结果**：首选方案、备选方案、不推荐方案及理由。
5. **落地方案**：变量、目标函数、约束、求解流程和验证设计。
6. **证据缺口与风险**：指出无法确认的内容、可能的偏差和下一步需要的数据。

## 约束

- 不编造论文中不存在的模型、数据、数值、引用或实验结果。
- 保留每条关键结论的来源；有页码、段落或用户文件名时一并标注。
- 不因为某个方法出现次数多就直接判定它最优。
- 明确区分相关性、预测能力和因果解释能力。
- 不能仅凭摘要确认公式或参数时，使用“摘要未说明”，不要补写看似完整的细节。
- 推荐模型时同时说明验证指标和失败条件，避免只给模型名称。
- 若用户要求数学建模竞赛方案，优先给出可复现、可解释且能验证约束的方案。
